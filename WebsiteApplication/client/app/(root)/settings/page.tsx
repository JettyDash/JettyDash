import Link from "next/link";

import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Settings | JettyDash",
};

export default async function Settings({ }) {
  return (
    <div>
      <h1>Settings</h1>
    </div>
  );
}
